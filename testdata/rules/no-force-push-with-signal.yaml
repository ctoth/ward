when: |
  tool == "Bash"
  && input.commands.exists(c, c.name == "git" && c.full.matches("^git\\s+push\\s+.*--force"))
  && !("force-push" in session.signals)
action: deny
message: "Force-push requires explicit permission. Use 'ward allow force-push' to override."
