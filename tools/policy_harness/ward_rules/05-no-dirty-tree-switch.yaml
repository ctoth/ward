when: |
  tool == "Bash"
  && repo.in_git
  && (repo.has_staged || repo.has_unstaged)
  && input.commands.exists(c,
       c.name == "git"
       && c.git_category in ["switch", "integrate", "history_rewrite"]
     )
action: deny
message: "Switching/integration/history rewrite requires a clean tree."
