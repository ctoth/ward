[project]
name = "ward-policy-harness"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "hypothesis>=6.135.0",
  "pytest>=8.3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
