when: |
  tool == "WebFetch"
  && input.url.matches("github\\.com/[^/]+/[^/]+/(blob|tree|raw)/")
  && !("github-raw-fetch" in session.signals)
action: deny
message: "Clone the repo instead of fetching raw files. Run: ward allow github-raw-fetch"
