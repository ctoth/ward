when: |
  tool == "Bash"
  && input.commands.exists(c,
       c.name == "git"
       && c.git_subcommand == "add"
       && c.git_args.exists(a, a in [".", "-A", "--all"])
     )
  && !("git-add-all" in session.signals)
action: deny
message: "Bulk staging is forbidden. Run: ward allow git-add-all"
